# Autoresearch Logs

Put new training, generation, evaluation, and helper-command logs here.

Avoid creating new root-level `run*.log`, `fid*.log`, or `gen*.log` files.
Existing root logs are legacy artifacts and may still be referenced by current
handoffs.
