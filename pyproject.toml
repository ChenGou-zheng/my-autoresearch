[project]
name = "assignment3"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "click",
    "matplotlib",
    "ninja",
    "numpy",
    "pillow",
    "psutil",
    "requests",
    "scipy",
    "torch",
    "torchvision",
    "tqdm",
]
