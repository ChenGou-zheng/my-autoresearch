# Plan

## Objective

Use `project.md` to define the current project objective.

## Strategy

1. Establish a baseline with the current project pipeline.
2. Try one bounded change at a time.
3. Evaluate with the primary metric from `project.md`.
4. Keep changes that improve the metric without adding fragile complexity.
5. Record each meaningful attempt in `experiment_journal.md` and `results.tsv`.
